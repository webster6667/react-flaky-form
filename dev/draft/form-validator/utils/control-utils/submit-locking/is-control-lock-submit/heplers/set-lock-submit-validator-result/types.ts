import { StaticValidator, HookProps } from './../../../../../../types';

export type SetLockSubmitBtnValidatorResult = (
  validator: StaticValidator,
  hooksData: HookProps,
  // shouldLockSubmitBtn: boolean,
  // shouldCheckValidatorSettings?: boolean,
) => void;

import { ControlProps, FormProps, SetFormProps } from "../types";
export declare const addControlSetting: (control: ControlProps, controlName: string, form: FormProps, formIndex: number | null, controlIndex: number | null, setForm: SetFormProps) => boolean;
